public class Permutation {
    private int numberOfChoice;
    private int inputFound;
    public static void main(String[] args) {
        if (args.length != 1)
            throw new IllegalArgumentException("Args length must be 1!!");
        int numberOfChoice = 0;
        int inputFound = Integer.parseInt(args[0]);

    }
}
